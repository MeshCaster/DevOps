name: Publish NuGet package

on:
  workflow_call:
    inputs:
      dotnet-version:
        description: .NET SDK version to use
        type: string
        default: "8.0.x"
      project-path:
        description: Path to the .csproj file or directory
        type: string
        default: "."
      configuration:
        description: Build configuration
        type: string
        default: "Release"
      nuget-source:
        description: NuGet source URL
        type: string
        default: "https://api.nuget.org/v3/index.json"
    secrets:
      nuget-api-key:
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.dotnet-version }}

      - name: Restore dependencies
        run: dotnet restore ${{ inputs.project-path }}

      - name: Build
        run: dotnet build ${{ inputs.project-path }} -c ${{ inputs.configuration }} --no-restore

      - name: Test
        run: dotnet test ${{ inputs.project-path }} -c ${{ inputs.configuration }} --no-build --verbosity normal

      - name: Pack
        run: dotnet pack ${{ inputs.project-path }} -c ${{ inputs.configuration }} --no-build -o ./nupkgs

      - name: Publish to NuGet
        run: dotnet nuget push ./nupkgs/*.nupkg --api-key ${{ secrets.nuget-api-key }} --source ${{ inputs.nuget-source }} --skip-duplicate
