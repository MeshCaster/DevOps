# universal-mep-site — Next.js reverse proxy for the tools/nginx stack.
#
# Upstream: container `universal-mep-site:3000` on the `meshcaster` network.
# Next.js serves its own static assets (/_next/*, images), so this is a plain
# single-upstream proxy — no static-file block needed.
#
# Domain: universal.in-vent.online

# ---------- HTTP :80 — ACME challenge + redirect to HTTPS ----------
server {
    listen 80;
    listen [::]:80;
    server_name universal.in-vent.online;

    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://$host$request_uri; }
}

# ---------- HTTPS :443 — proxy to the Next.js container ----------
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name universal.in-vent.online;

    ssl_certificate         /etc/letsencrypt/live/universal.in-vent.online/fullchain.pem;
    ssl_certificate_key     /etc/letsencrypt/live/universal.in-vent.online/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/universal.in-vent.online/chain.pem;
    include /etc/nginx/snippets/ssl-params.conf;

    client_max_body_size 20M;

    location / {
        proxy_pass http://universal-mep-site:3000;
        include /etc/nginx/snippets/proxy-common.conf;
    }
}
